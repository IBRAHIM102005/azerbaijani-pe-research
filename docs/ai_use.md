# AI-use disclosure

Per the course brief (DLE-AI-202 v1.6), substantial AI use must be
disclosed, and AI-generated code must be human-reviewed and tested before
merge. Fill in per-component disclosure here as the project proceeds:

| Component | AI assistance used | Human review performed |
|---|---|---|
| Config auditor (`scripts/audit_configs.py`) | Yes | TODO: reviewer name + date |
| Parameter auditor (`scripts/audit_parameters.py`) | Yes | TODO |
| Metadata collector (`scripts/collect_metadata.py`) | Yes | TODO |
| Determinism utilities (`src/reproducibility/determinism.py`) | Yes | TODO |
| Checkpoint integration tests (`tests/integration/`) | Yes | TODO |
| Release verifier (`scripts/verify_release.py`) | Yes | TODO |
| CI workflow (`.github/workflows/ci.yml`) | Yes | TODO |

This table must be completed by each module owner before submission; it is
a release-blocking check per the brief's automatic-deduction rules.
