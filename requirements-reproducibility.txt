# Reproducibility/runtime dependencies shared across data, model, training,
# evaluation and integration tooling.
#
# Python packages below are pinned where the repository already has a stable
# cross-platform version. The A100-specific Python/PyTorch/CUDA/cuDNN/driver
# runtime is frozen from the ACTUAL target server after benchmarking with:
#
#   python scripts/m3_freeze_environment.py --require-cuda --require-bf16
#
# The resulting configs/hardware/a100_environment.json is then enforced by
# scripts/m3_server_preflight.py before headline training. This avoids guessing
# a CUDA/PyTorch build that may not match the booked A100 image.

datasketch==2.0.0
matplotlib==3.10.8
numpy==2.4.0
pyarrow==25.0.1
protobuf==7.35.1
pytest==8.4.2
PyYAML==6.0.3
sentencepiece==0.2.1

# Minimum import compatibility before the target A100 image is known.
# The exact torch build is recorded and enforced by a100_environment.json
# before the 25-run scientific matrix starts.
torch>=2.1
